# 환경변수 (절대 올리면 안됨)
.env

# PEM 키 (절대 올리면 안됨)
*.pem

# 쿠키 및 로그 (개인정보 포함)
tistory_cookies.json

# 런타임 데이터
daily_log.json

# 브라우저 persistent 데이터 (카카오 기기 인증 쿠키 포함, 절대 올리면 안됨)
browser_data/

# 생성된 HTML 초안/발행본 (대용량)
articles/*/draft/
articles/*/published/
articles/draft/
articles/published/

# 실험/폐쇄된 시스템 잔재
claude2/

# 디버그 스크린샷
*.png

# Python
__pycache__/
*.py[cod]
venv/
.venv/

# 로컬 전용 스크립트 (서버에서 불필요)
setup_browser.py
debug_login.py

# Claude Code 로컬 설정
.claude/

# 에디터
.vscode/
.idea/
mycron.txt
